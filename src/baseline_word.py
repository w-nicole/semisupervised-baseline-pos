
import os
from transformers import BertForMaskedLM, BertModel

if __name__ == '__main__':
    model_name = 'bert-base-multilingual-cased'
    assert not model.hparams.concat_all_hidden_states and self.hparams.feature_layer == -1,\
        "Assumptions for calculating huggingface accuracy do not hold."
    